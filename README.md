Тестовое задание
В приложении вы найдёте Unity-проект с заранее подготовленной архитектурой и
основными системами.
Ваша задача — реализовать простой бой на арене, где персонажи разных команд
двигаются, атакуют друг друга и при «смерти» переходят на сторону противника.

Задача 1. Класс персонажа и модель состояния
Сделать ядро: один класс/модель персонажа, без или с завязкой на MonoBehaviour.
Обязательно:
● Персонаж имеет:
○ Идентификатор команды
○ Свою Расу
○ Систему здоровья
○ Систему атаки
○ Обработчик состояний / система для поведения

Задача 2. Передвижение и атака
Сделать простую арену и добавить базовый геймплей поверх модели персонажа из
Задачи 1.
Управление игроком
● Платформа: мобильная, инпут тоже под мобильные устройства
● Можно использовать предоставленный сервис ввода (который использует Input
System) или можно перейти на старый InputManager. Также разрешено
использовать сторонние ассеты для виртуального джойстика
● Как именно реализован ввод (джойстик, тап-перемещение, свайпы) — на
усмотрение кандидата, главное: персонаж должен двигаться по плоскости и
ориентироваться в сторону атаки/движения.
Атака и смена стороны
● У игрока должна быть кнопка атаки
● Атака снимает HP у целей в зоне действия (имплементация радиуса атаки - на
выбор кандидату)
● При достижении 0 HP:
○ цель сразу переходит в команду атакующего
○ её HP устанавливается в какое-то базовое значение
○ дальше она живёт и ведёт себя как персонаж новой стороны.

Задача 3. ИИ
Добавить несколько ботов, использующих тот же класс персонажа и те же состояния,
что и игрок.
Поведение ботов
● В обычном состоянии боты двигаются по арене между случайными точками
● Если бот «видит» рядом персонажа другой команды:
○ бот начинает приближаться к этой цели;
○ при достаточном расстоянии вызывает ту же атаку, что и игрок
● Если бот атакой «убивает» цель, она также сразу переходит в его команду и
продолжает жить уже за новую сторону.
● Большим плюсом будет если персонажи одной команды будут держатся вместе и
передвигаться группой.

Примечания
● Красота графики не является приоритетом. То-есть сама графика, Анимации, VFX
и сложный UI значения не имеют. Достаточно понятных примитивов, на усмотрение
Кандидата.
● Главное внимание уделяется самой играбельности, качеству кода и его удобству
поддержки и расширяемости.
● Мы работаем в динамичной среде, поэтому важно, чтобы игру можно было быстро
развивать и изменять.
Что прислать
1. Unity-проект без папки Library и других временных файлов.
2. Короткий README:
○ какую сцену открыть, как запустить
○ опционально - пара комментариев по своей работе
Проект можно отправить в формате zip-архива или ссылку на его Git-репозиторий

========================================================

Огляд реалізованих систем
🏥 Система здоров'я (Health)
HealthData - дата-модель, яка зберігає поточні та максимальні значення HP і Armor. Логіка розрахунку шкоди: спочатку витрачається броня, потім HP.
HealthProvider - MonoBehaviour-компонент, який надає інтерфейс для роботи з здоров'ям юніта. Підтримує нанесення шкоди, лікування, перевірку життя та воскресіння. Викидає івент OnDeath при смерті.
IHealthProvider - інтерфейс для взаємодії з системою здоров'я.
🎯 Система навичок (Skills)
ISkill - базовий інтерфейс для всіх скілів. Визначає тип, дальність, кулдаун та методи використання.
BaseSkill - абстрактний клас з базовою логікою кулдауну та перевірки готовності.
SkillContext - контекст виконання скілу (позиція кастера, ціль, тощо).
SkillData - конфігураційні дані скілу (шкода, радіус, дальність, кулдаун).
SkillExecutor - компонент який керує набором скілів юніта, обробляє їх тіки та виконання.
Реалізації скілів:

ShootSkill - стрільба снарядом через ProjectileSpawner
DamageAoESkill - area damage навколо точки
DamageDashSkill - dash з нанесенням шкоди
WideMeleeSkill - широкий ближній удар

Контролери скілів:

PlayerShootController - контролює стрільбу гравця
EnemyShootController - AI для ворогів, який знаходить найближчу ціль та стріляє

💥 Система снарядів (Projectiles)
Projectile - снаряд з логікою нанесення шкоди при зіткненні та повернення в пул.
ProjectileMover - рух снаряда по прямій з обмеженням дистанції.
ProjectileSpawner - factory для створення снарядів з використанням object pooling.
🏃 Система руху (Movement)
IMovementController - інтерфейс для керування рухом юніта.
MovementController - реалізація руху (імовірно через NavMesh або Transform).
PlayerInputController - зчитує input від гравця (тач/клавіатура) та передає команди в MovementController.
👥 Система команд (Team)
TeamSide - enum для визначення команди (TeamA, TeamB).
TeamMember - компонент який позначає приналежність юніта до команди.
TeamMaterialSwitcher - змінює матеріал юніта залежно від команди (візуальна ідентифікація).
🎮 Правила гри (GameRules)
GameRuleStrategy - базова стратегія для різних режимів гри.
GameRulesManager - менеджер який керує активним правилом гри та перевіряє умови перемоги.
VictoryResult - результат перевірки умов перемоги.
Стратегії правил:

TeamEliminationRule - перемога при знищенні всієї ворожої команди
TeamSwitchRule - режим де юніти можуть змінювати команду

⚙️ Конфігурації
RaceStatsConfig - ScriptableObject з характеристиками різних рас (HP, швидкість).
RaceSkillConfig - конфігурація скілів для кожної раси.
UnitRace - enum рас (Elf, Orc, Dwarf, Human).
🧪 Тестові компоненти
TestUnit - простий скрипт для ініціалізації юніта з конфігурацій.
TestProjectileSpawner - тестовий спавнер для відлагодження снарядів.
TestGameplay - тестування ігрових правил.

========================================================

Сцени для тестування механік:
1) Test_damage - сцена для перевірки нанесення урону (проджектайл потрібно вручну порухати на юніт, щоб нанести урон)
2) Test_movement - сцена для налаштування руху гравця
3) Test_projectileSpawner - сцена для перевірки роботи спавнера
4) Test_shooting - сцена для перевірки роботи інпут-системи для пострілу гравцем (там по лівій стороні екрану)
5) Test_navMesh - сцена для перевірки роботи навмешу (пробігаємість поля бою)
6) Test_enemyController - сцена для перевірки поведінки ботів (BotAIController)
7) Test_gameplay - сцена ля перевірки кінця гри
